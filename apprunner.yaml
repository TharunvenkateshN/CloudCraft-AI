version: 1.0
runtime: python311
build:
  commands:
    build:
      - echo "Build phase complete - dependencies handled in pre-run for Python3.11"
run:
  pre-run:
    - pip install -r requirements.txt
  command: python -m uvicorn src.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
